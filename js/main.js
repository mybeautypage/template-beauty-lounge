(function () { "use strict"; /* Leistungs-Karten: Accordion (Mobile) + Hover-Trigger (Desktop) */ document.querySelectorAll(".svc-card--toggle").forEach(function (card) { var btn = card.querySelector(".svc-link"); if (btn) { var url = btn.getAttribute("href"); var target = btn.getAttribute("target"); card.addEventListener("mouseenter", function () { btn.classList.add("is-triggered"); }); card.addEventListener("mouseleave", function () { btn.classList.remove("is-triggered"); }); if (url && url !== "#" && url !== "") { card.querySelectorAll(".svc-body, .svc-img").forEach(function (el) { el.style.cursor = "pointer"; el.addEventListener("click", function (e) { if (e.target.closest("a")) return; if (target === "_blank") window.open(url, "_blank"); else window.location.href = url; }); }); } } var header = card.querySelector(".svc-toggle"); if (header) { header.addEventListener("click", function () { if (card.classList.contains("is-open")) { card.classList.remove("is-open"); } else { document.querySelectorAll(".svc-card.is-open").forEach(function (c) { c.classList.remove("is-open"); }); card.classList.add("is-open"); } }); } }); /* Bento-/Standort-Karten: Hover-Trigger + Klick auf ganze Karte */ document.querySelectorAll(".card-hover").forEach(function (card) { var btn = card.querySelector(".btn-ghost"); if (!btn) return; var href = btn.getAttribute("href"); var target = btn.getAttribute("target"); card.addEventListener("mouseenter", function () { btn.classList.add("is-triggered"); }); card.addEventListener("mouseleave", function () { btn.classList.remove("is-triggered"); }); if (href && href !== "#" && href !== "") { card.style.cursor = "pointer"; card.addEventListener("click", function (e) { if (e.target.closest("a")) return; if (target === "_blank") window.open(href, "_blank"); else window.location.href = href; }); } }); /* Ghost-Trigger-Buttons (Standort-Karten) */ document.querySelectorAll(".btn-ghost").forEach(function (btn) { var href = btn.getAttribute("href"); if (!href || href === "#") return; btn.addEventListener("click", function (e) { if (btn.getAttribute("target") === "_blank") { e.preventDefault(); window.open(href, "_blank"); } }); }); /* Footer Untermenü (Mobile) */ document.querySelectorAll(".footer__menu--has-sub > a").forEach(function (link) { link.addEventListener("click", function (e) { if (window.innerWidth > 1024) return; e.preventDefault(); link.parentElement.classList.toggle("is-active"); }); }); /* Nav Untermenü Hover-Verzögerung */ var CLOSE_DELAY = 320; document.querySelectorAll(".nav .nav__item--sub").forEach(function (item) { var timer; item.addEventListener("mouseenter", function () { clearTimeout(timer); item.classList.add("submenu-open"); }); item.addEventListener("mouseleave", function () { timer = setTimeout(function () { item.classList.remove("submenu-open"); }, CLOSE_DELAY); }); });
})();
